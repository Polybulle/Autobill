//cmd test ret a = trace "log" unit() in unit().ret(a)

cmd test2 ret b =
  val x = box(Exp, match this.thunk().ret(c) -> unit().ret(c)) in
  copy x as (y,z) in
  y.unbox(Exp).thunk().bind y ->
  z.unbox(Exp).thunk().bind z ->
  tuple(y,z).ret(b)
