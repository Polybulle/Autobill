comput ID1 = this.inst1<X1:+>().ret( Fun X1 -> (Thunk X1) )
comput ID2 = this.inst2<X2:+>().ret( Fun X2 -> (Thunk X2) )

decl val x : ID1

val y = match this.inst2().ret(a) ->
    cmd-
    stk = this.ret(a)
    val = match this.call(z).ret(b) -> x.inst1().call(z).ret(b)
    end
