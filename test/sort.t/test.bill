decl type int : +
decl type char : +
decl val z : int
decl val succ : (fun (thunk int) int)

data list (c : +) =
  case nil()
  case cons(c, (list c))

val lz = cons(z, cons( z, nil() ))

decl val map : (fun (thunk (list char)) (closure (fun (thunk char) int)) (list int))
