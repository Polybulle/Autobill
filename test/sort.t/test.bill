decl type int : +
decl type char : +
decl val z : int
decl val succ : (fun int -> (shift- int))

data list (c : +) =
  case nil()
  case cons(c, (list c))

val lz = cons(z, cons( z, nil() ))

decl val map : (fun (shift+ (fun int -> (shift- char))) (list int) -> (shift- (list char)))
